package Algorithms;

public class Insertion {/*
    public static void sort(Comparable[] a) {
        // Сортировка a[] по возрастанию.
        int N = a.length;
        for(int i = 1; i < N; i++) {
            // Вставка a[i] среди a[i-1], a[i-2] ... .
            for(int j = i; j > 0 && less(a[j], a[j-1]); j--)
                exch(a, j, j-1)
        }
    }*/
}
